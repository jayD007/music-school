const baseUrl = '/music-school/';

export const routes = {
  home: baseUrl,
  about: `${baseUrl}about`,
  lessons: `${baseUrl}lessons`,
  contact: `${baseUrl}contact`,
  gallery: `${baseUrl}gallery`,
};
