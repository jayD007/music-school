import { Page } from '../../../types';
import { routes } from '../../../routes';

export const pages: Page = [{ title:'Products', route:routes.home },{ title:'Pricing', route:'' }, { title:'About', route: routes.about },{ title:'Contact', route:'' } ];